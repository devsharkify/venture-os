"""Local stub for the Emergent-platform-private `emergentintegrations` package.

The original repo was built inside the Emergent IDE which served this package
from a private index. On public PyPI it does not exist. This stub provides
the import surface so the backend boots; any LLM call will raise
NotImplementedError until you wire a real provider (Gemini, OpenAI, etc).
"""
